# 🧪 Test-Driven Development — Doctests

Doctest files for the `python-test_driven_development` module.

## Files

| File | Description |
|------|-------------|
| `0-add_integer.txt` | Tests for `add_integer()` function |
| `2-matrix_divided.txt` | Tests for `matrix_divided()` function |
| `3-say_my_name.txt` | Tests for `say_my_name()` function |
| `4-print_square.txt` | Tests for `print_square()` function |
| `5-text_indentation.txt` | Tests for `text_indentation()` function |

## Usage

Run doctests with:

```bash
python3 -m doctest -v <module_name>.py
```

---

*Part of [holbertonschool-higher_level_programming](../../README.md)*
