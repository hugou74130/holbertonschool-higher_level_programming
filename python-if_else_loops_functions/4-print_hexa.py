#!/usr/bin/python3

# Boucle à travers les nombres de 0 à 99
for i in range(100):# Boucle pour
    # Convertit le nombre en format hexadécimal et l'imprime avec un espace final
    print("{:x}".format(i), end=" ")    # Affiche à l'écran
